# PostgreSQL Connection -- defaults (credentials prompted at startup if blank)
db.host=localhost
db.port=5432
db.name=
db.username=
db.password=

# Connection Pool (HikariCP)
db.pool.size=10
db.pool.connection.timeout=30000
db.pool.idle.timeout=600000
db.pool.max.lifetime=1800000

# JPA-style logging hints (informational only -- not processed by Spring)
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Application
app.name=Tax Calculation System
app.version=1.0.0
